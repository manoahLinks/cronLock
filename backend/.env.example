# Example environment configuration for the X402-enabled API.
#
# @remarks
# This file documents the required and optional environment variables
# used by the application. Copy this file to `.env` and adjust values
# as needed for your environment.

# Port on which the HTTP server will listen.
PORT=8787

# Cronos network identifier.
# Supported values include:
# - cronos-testnet
# - cronos-mainnet
NETWORK=cronos-testnet

# Address that will receive payments for protected resources.
# This value is required at runtime.
MERCHANT_ADDRESS=0xMERCHANT_ADDRESS

# Price required to access the protected resource, expressed in base units.
# Example: "1000000" for 1 USDC with 6 decimals.
PRICE_BASE_UNITS=1000000
